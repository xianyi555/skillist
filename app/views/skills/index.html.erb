<% @skills.each do |skill| %>
  <ul>
    <li><strong>Title:</strong><%= link_to "#{skill.name}", skill_path(skill.id) %></li>
    <li><strong>Content:</strong> <%= skill.description %> </li>
    <li><strong>Cost:$</strong> <%= skill.cost %> </li>
    <br>
  </ul>
<% end %>
<%= link_to "Back to homepage", root_path %>